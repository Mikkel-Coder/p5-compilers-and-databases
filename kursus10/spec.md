Get (receive) info:
```json
{
    "method": "get",
    "attribute": ""
    "scope": <SCOPE>
}
```

Post (add) info:
```json
{
    "method": "post",
    "value": ""
    "scope": <SCOPE>
}

```

Delete info:
```json
{
    "method": "delete"
    "scope": <SCOPE>
}
```

```
SCOPE = "<host|network>"
```
